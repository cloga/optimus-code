{
  "$schema": "./available-agents.schema.json",
  "_schema_version": 1,
  "_comment": "Optional project-level override. Copy to .optimus/config/available-agents.json to override engine settings. Add 'available_models' per engine to restrict model choices (e.g. \"available_models\": [\"claude-opus-4.6-1m\", \"gpt-5.4\"]).",
  "engines": {
    "claude-code": {
      "timeout": { "heartbeat_ms": 600000, "activity_ms": 300000 }
    },
    "github-copilot": {
      "timeout": { "heartbeat_ms": 600000 }
    },
    "qwen-code": {
      "timeout": { "heartbeat_ms": 600000, "activity_ms": 300000 },
      "acp": { "path": "auto", "args": ["--acp"] },
      "_comment": "path 'auto' = auto-discover from VS Code extensions"
    }
  }
}
