{
  "$schema": "./available-agents.schema.json",
  "_schema_version": 1,
  "_comment": "Default user-level engine configuration. Models are auto-resolved by each engine CLI. Add 'available_models' per engine to explicitly restrict model choices.",
  "engines": {
    "claude-code": {
      "timeout": { "heartbeat_ms": 600000, "activity_ms": 300000 }
    },
    "github-copilot": {
      "timeout": { "heartbeat_ms": 600000, "activity_ms": 300000 }
    },
    "qwen-code": {
      "timeout": { "heartbeat_ms": 600000, "activity_ms": 300000 },
      "acp": { "path": "auto", "args": ["--acp"] },
      "_comment": "path 'auto' = auto-discover from VS Code extensions"
    }
  }
}
