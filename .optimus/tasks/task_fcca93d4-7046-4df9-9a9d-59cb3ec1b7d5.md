Please perform a comprehensive codebase scan of `src/` (especially `src/adapters/`, `src/mcp/`, `src/managers/`) and the TS files. Look for any instances where output files, logs, temp data, or debug dumps are written to the workspace root or global directories (e.g., `temp_data`, `./qa_report.md`, `./delegation_result.txt`) instead of isolated strictly within the `.optimus/` folder of the associated project workspace. If you find any loose paths, refactor them to use `path.join(workspacePath, '.optimus', ...)` following best practices. Modify the files to fix these issues. Emit a complete summary of modified files and legacy path issues resolved.