Review the recent refactoring where we moved the VS Code Adapters to be purely Node.js compatible, and linked the MCP `worker-spawner.ts` to directly instantiate `GitHubCopilotAdapter`. Formulate a comprehensive unit test and integration test plan for these changes. The scope is defined in `temp_data/mcp_refactor_test_plan.md`.